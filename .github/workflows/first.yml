name: Test

on:
  pull_request:
    types: [opened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: CHECKOUTT LS
        run: ls
      - uses: samifiaz/gh-action-docker@master
      - name: MY ACTION LS
        run: ls
